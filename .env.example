DATABASE_URL=
AUTH_SECRET=

# Contact form (lib/actions/contact.ts). Get a key at resend.com/api-keys.
# Sending from contact@aimify.app also requires verifying aimify.app as a
# sending domain in Resend's dashboard first (adds DNS records at
# Cloudflare, same place as the Vercel/mail records).
RESEND_API_KEY=

# Company logo and product images (lib/blob.ts). Create a PUBLIC Blob store
# in the Vercel dashboard (Storage > Blob), connect it to this project, and
# copy its read-write token here. Without it, uploads show a friendly
# "not available" message instead of failing.
BLOB_READ_WRITE_TOKEN=

# Pre-launch "coming soon" gate (see proxy.ts). Leave LAUNCH_GATE_ENABLED
# unset/false while developing -- the real site stays visible to everyone,
# including locally. Set it to "true" on Vercel when you want the public to
# see the countdown instead of the app.
LAUNCH_GATE_ENABLED=false
LAUNCH_GATE_BYPASS_SECRET=
# ISO date-time the countdown counts down to, e.g. 2026-10-15T09:00:00Z.
# Public (client-side) because the countdown timer runs in the browser.
NEXT_PUBLIC_LAUNCH_AT=

# Daily subscription emails (app/api/cron/subscription-emails). Vercel Cron
# sends this as a Bearer token; the route refuses to run without it. Set any
# long random string in the Vercel project settings.
CRON_SECRET=
